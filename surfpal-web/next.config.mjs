/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com'], // Add the required hostname here
    },
  };
  
export default nextConfig;
  